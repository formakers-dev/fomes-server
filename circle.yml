machine:
  timezone:
    Asia/Tokyo
  node:
    version: 6.11.0

dependencies:
  pre:
    - rm -rf ./node_modules
  cache_directories:
    - ~/.npm

deployment:
  development:
    branch: development
    heroku:
      appname: appbee-server-dev
  staging:
    branch: release
    heroku:
      appname: appbee-server-stg